{
  "name": "world_wise_backend",
  "version": "1.0.0",
  "description": "Provides API endpoints for cities data",
  "main": "server.js",
  "scripts": {
    "start": "chmod +x node_modules/.bin/json-server && node_modules/.bin/json-server --watch data/cities.json --port $PORT"
  },
  "keywords": [
    "backend",
    "json-server",
    "cities"
  ],
  "author": "Diogo",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
